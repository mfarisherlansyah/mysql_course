CREATE TABLE shirts(shirt_id INT NOT NULL AUTO_INCREMENT
                   , article VARCHAR(!00)
                   , colour VARCHAR(100)
                   , shirt_size VARCHAR(10)
                   , last_worn INT
                   , PRIMARY KEY (shirt_id));